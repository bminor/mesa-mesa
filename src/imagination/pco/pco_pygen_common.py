# Copyright © 2024 Imagination Technologies Ltd.
# SPDX-License-Identifier: MIT

from enum import Enum, auto

_ = None

prefix = 'pco'

class BaseType(Enum):
   bool = auto()
   uint = auto()
   enum = auto()
